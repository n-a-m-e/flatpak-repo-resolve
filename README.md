# Flatpak repository

```bash
sudo flatpak remote-add --if-not-exists flatpak-repo-resolve https://n-a-m-e.github.io/flatpak-repo-resolve/index.flatpakrepo
```
```bash
sudo flatpak install flatpak-repo-resolve com.blackmagic.Resolve
```
